# Étape 4 — Pattern State (reservation-service)

Même base que l’étape 3, avec le pattern **State** pour `cancel` / `complete` sur les réservations.

Voir `reservation-service/DESIGN_PATTERN.md`.

Build : `cd etape-4 && mvn -DskipTests package` (Java 17).
